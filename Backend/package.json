{
  "name": "backend",
  "version": "1.0.0",
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "jwt-decode": "^4.0.0"
  },
  "scripts": {
    "start": "json-server --watch db.json --port 5000"
  }
}
